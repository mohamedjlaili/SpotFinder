import 'package:flutter/material.dart';

class FormStyles {
  /// A reusable generic Text Style used for errors in the SpotFinder text fields.
  static TextStyle get errorTextStyle {
    return const TextStyle(color: Colors.orange, fontSize: 15);
  }
}
